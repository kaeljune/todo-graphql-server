# Todo GraphQL Server

Installation

```sh
$ https://github.com/kaeljune/todo-graphql-server.git [name of your app]
$ cd [name of your app]
$ rm -rf .git
$ yarn
$ meteor npm install --save bcrypt
$ meteor
```
# todo-graphql-server
